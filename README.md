# Интерактивный каталог десертов

Веб-приложение для презентации продукции производителя десертов с возможностью фильтрации и выгрузки коммерческих предложений в формате PDF.

## Технологический стек

- **Backend:** Python 3.10+, FastAPI
- **Frontend:** React 18, TypeScript
- **База данных:** SQLite (для разработки) / PostgreSQL (для продакшена)
- **Генерация PDF:** ReportLab / WeasyPrint
- **ORM:** SQLAlchemy

## Функциональность

- 📱 Интерактивный каталог с карточками товаров
- 🔍 Поиск и фильтрация по категориям
- 📄 Детальная информация о каждом десерте
- 📥 Генерация PDF каталогов с настройками экспорта
- 🔧 Админ-панель для управления контентом

## Быстрый старт

### Docker Compose (рекомендуется для деплоя)

Самый простой способ запустить проект в Docker:

```bash
# 1. Запустите все сервисы (автоматически создаст .env если нужно)
make docker-up

# 2. Инициализируйте базу данных
make docker-init-db

# 3. Откройте в браузере
# Frontend: http://localhost:3000
# Backend API: http://localhost:8000/docs
```

Или вручную:
```bash
docker-compose up -d
docker-compose exec backend python init_db.py
```

Для production с PostgreSQL:
```bash
make docker-up-prod
```

Подробнее см. [DOCKER.md](DOCKER.md) и [QUICKSTART_DOCKER.md](QUICKSTART_DOCKER.md)

### Удаленный деплой

Для деплоя на удаленный сервер:

```bash
# 1. Настройте конфигурацию
cp deploy.env.example deploy.env
# Отредактируйте deploy.env с данными вашего сервера

# 2. Задеплойте
make deploy
```

Подробнее см. [DEPLOY.md](DEPLOY.md)

### Использование Makefile (для локальной разработки)

Самый простой способ запустить проект:

```bash
# Полная настройка проекта (первый раз)
make setup

# Запустить все сервисы в фоне
make run-all-bg

# Проверить статус
make status

# Остановить все сервисы
make stop

# Показать все доступные команды
make help
```

### Ручная установка и запуск

#### Backend

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:
```bash
# macOS/Linux:
source venv/bin/activate

# Windows:
venv\Scripts\activate
```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

5. Инициализируйте базу данных с тестовыми данными:
```bash
python init_db.py
```

6. Запустите сервер:
```bash
uvicorn main:app --reload
```

API будет доступен по адресу: http://localhost:8000
Документация API (Swagger): http://localhost:8000/docs

#### Frontend

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev-сервер:
```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

## Команды Makefile

| Команда | Описание |
|---------|----------|
| `make help` | Показать справку по всем командам |
| `make setup` | Полная настройка проекта (установка + БД) |
| `make install` | Установить все зависимости |
| `make run-all-bg` | Запустить все сервисы в фоне |
| `make run-backend` | Запустить только бэкенд (интерактивно) |
| `make run-frontend` | Запустить только фронтенд (интерактивно) |
| `make stop` | Остановить все фоновые процессы |
| `make restart` | Перезапустить все сервисы |
| `make status` | Показать статус сервисов |
| `make init-db` | Инициализировать БД с тестовыми данными |
| `make clean` | Очистить временные файлы |
| `make clean-all` | Полная очистка (включая venv и node_modules) |
| `make logs-backend` | Показать логи бэкенда |
| `make logs-frontend` | Показать логи фронтенда |

## Структура проекта

```
Cataloge_curs/
├── backend/                    # FastAPI приложение
│   ├── app/
│   │   ├── __init__.py
│   │   ├── models.py          # Модели данных (SQLAlchemy)
│   │   ├── schemas.py         # Pydantic схемы для валидации
│   │   ├── database.py        # Настройка подключения к БД
│   │   ├── api/               # API endpoints
│   │   │   ├── desserts.py   # CRUD операции для десертов
│   │   │   └── pdf.py         # Генерация PDF
│   │   └── pdf/               # Модуль генерации PDF
│   │       └── generator.py   # Логика создания PDF файлов
│   ├── main.py                # Точка входа приложения
│   ├── init_db.py             # Скрипт инициализации БД
│   └── requirements.txt       # Python зависимости
├── frontend/                  # React приложение
│   ├── src/
│   │   ├── components/        # React компоненты
│   │   │   ├── Catalog.tsx           # Главная страница каталога
│   │   │   ├── DessertCard.tsx       # Карточка товара
│   │   │   ├── DessertModal.tsx      # Модальное окно с деталями
│   │   │   ├── PDFExportModal.tsx    # Модальное окно экспорта PDF
│   │   │   └── AdminPanel.tsx        # Админ-панель
│   │   ├── services/
│   │   │   └── api.ts         # API клиент
│   │   ├── types.ts           # TypeScript типы
│   │   ├── App.tsx            # Главный компонент
│   │   └── main.tsx           # Точка входа
│   ├── package.json
│   ├── vite.config.ts         # Конфигурация Vite
│   └── tailwind.config.js     # Конфигурация Tailwind CSS
└── README.md
```

## Основные функции

### Каталог товаров
- Отображение всех десертов в виде сетки карточек
- Поиск по названию и описанию
- Фильтрация по категориям
- Выбор товаров для экспорта в PDF (чекбоксы)
- Анимации при наведении на карточки

### Детальная информация
- Модальное окно с полной информацией о десерте
- Отображение: название, описание, состав, КБЖУ, вес/фасовка
- Красивое оформление с поддержкой изображений

### Генерация PDF
- Выбор товаров для экспорта
- Настройки экспорта:
  - Включить/выключить состав
  - Включить/выключить КБЖУ
  - Добавить титульный лист с логотипом и контактами
- Автоматическая генерация и скачивание PDF файла

### Админ-панель
- Просмотр всех десертов (включая неактивные)
- Создание новых десертов
- Редактирование существующих
- Удаление десертов
- Управление статусом активности

## API Endpoints

### Десерты
- `GET /api/desserts/` - Получить список десертов (с фильтрацией)
- `GET /api/desserts/{id}` - Получить десерт по ID
- `GET /api/desserts/categories` - Получить список категорий
- `POST /api/desserts/` - Создать новый десерт
- `PUT /api/desserts/{id}` - Обновить десерт
- `DELETE /api/desserts/{id}` - Удалить десерт

### PDF
- `POST /api/pdf/export` - Генерация PDF каталога

## Модель данных

### Dessert
- `id` (Integer) - Уникальный идентификатор
- `title` (String) - Название десерта
- `category` (String) - Категория
- `image_url` (String, optional) - URL изображения
- `description` (Text, optional) - Описание
- `ingredients` (Text, optional) - Состав
- `calories` (Float, optional) - Калорийность на 100г
- `proteins` (Float, optional) - Белки на 100г
- `fats` (Float, optional) - Жиры на 100г
- `carbs` (Float, optional) - Углеводы на 100г
- `weight` (String, optional) - Вес/фасовка
- `is_active` (Boolean) - Активен ли в каталоге

## Загрузка изображений

Проект поддерживает загрузку изображений на сервер:

- **Формат:** JPG, JPEG, PNG, WEBP, GIF
- **Максимальный размер:** 10MB
- **Хранение:** `uploads/images/` на сервере
- **Доступ:** Изображения доступны по URL `/static/images/{filename}`

### Использование в админ-панели:

1. Откройте форму создания/редактирования десерта
2. В поле "Изображение" выберите файл для загрузки
3. Изображение автоматически загрузится и отобразится в превью
4. Также можно ввести URL внешнего изображения

## Аутентификация и авторизация

Проект использует JWT токены для аутентификации:

- **Регистрация:** `/register` - создание нового пользователя
- **Вход:** `/login` - получение JWT токена
- **Защита:** Админ-эндпоинты требуют аутентификации и прав администратора

### Создание администратора

Для создания первого администратора используйте скрипт:

```bash
cd backend
source venv/bin/activate
python create_admin.py <username> <email> <password>
```

Пример:
```bash
python create_admin.py admin admin@example.com admin123
```

### Роли пользователей

- **Обычный пользователь:** Может просматривать каталог и экспортировать PDF
- **Администратор:** Полный доступ, включая управление десертами через админ-панель

## Security & Deployment

### Security
- See [SECURITY.md](SECURITY.md) for security best practices
- All file uploads require authentication
- PDF export requires authentication
- Admin endpoints are protected
- Path traversal protection implemented
- Input validation on all endpoints

### Deployment
- See [DEPLOYMENT.md](DEPLOYMENT.md) for deployment instructions
- See [PRODUCTION_CHECKLIST.md](PRODUCTION_CHECKLIST.md) before going live
- Docker configuration included
- Environment variables required (see `backend/env.example`)

### Important Notes

- **SECRET_KEY**: Must be set in production (min 32 characters)
- **Database**: SQLite for development, PostgreSQL recommended for production
- **CORS**: Configure `ALLOWED_ORIGINS` for your domain
- **Environment**: Set `ENVIRONMENT=production` in production
- JWT tokens valid for 30 days
- Uploaded images stored in `uploads/` directory (excluded from git)

